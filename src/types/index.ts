export interface UserInfo {
    name: string;
    email: string;
    gender: string;
    phone: number;
    hasunsubscribed: boolean;
};

export interface MessageStructure {
    medium: string;
    email: string;
    phone: string;
    name: string;
}